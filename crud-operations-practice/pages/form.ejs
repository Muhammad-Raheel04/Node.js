<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/form.css">
</head>

<body>
    <div class="container">
    <h1>Student CRUD Form</h1>
    <form action="/create" method="POST">
        <input type="hidden" name="id" value="<%= editStudent?._id || ''%>">
        <input type="text" name="name" value="<%= editStudent?.name || ''%>" placeholder="Student Name" required>
        <input type="number" name="age" value="<%= editStudent?.age || ''%>" placeholder="Age" required>
        <input type="text" name="course" value="<%= editStudent?.course || ''%>" placeholder="Course" required>
        <button type="submit">Add Student</button>
    </form>

    <h2>Student List</h2>
    <ul>
        <% if(students.length>0) { %>
            <% students.forEach(student=>{%>
                <li>
                    <%=student.name%> | <%=student.age%> |<%=student.course%>
                    <a href="/edit/<%=student._id%>">Edit</a>
                    <a href="/delete/<%=student._id%>">Delete</a>
                </li>
            <%})%>
        <%}else{%>
            <li>No students yet</li>
        <%}%>    
    </ul>
    </div>
</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Student CRUD Form</h1>
    <form action="/create" method="POST">
        <input type="hidden" name="id" value="<%= editStudent?._id || ' '%>">
        <input type="text" name="name" value="<%= editStudent?.name || ' '%>" placeholder="Student Name" required>
        <input type="number" name="age" value="<%= editStudent?.age || ' '%>" placeholder="Age" required>
        <input type="text" name="course" value="<%= editStudent?.course || ' '%>" placeholder="Course" required>
        <button type="submit">Add Student</button>
    </form>

    <h2>Student List</h2>
    <ul>
        <% if(students.length>0) { %>
            <% students.forEach(student=>{%>
                <li>
                    <%=student.name%> | <%=student.age%> |<%=student.course%>
                    <a href="/edit/<%=student._id%>">Edit</a>
                    <a href="/delete/<%=student._id%>">Delete</a>
                </li>
            <%})>%>
        <%}else{%>
            <li>No students yet</li>
        <%}%>    
    </ul>
</body>

</html>